fund=false
; Repository-local npm configuration to reduce noise and prevent automatic audits
; Disable automatic npm audit runs for local installs and CI in this repo
audit=false
; Disable funding messages to avoid npm prompting for sponsorships
fund=false
; Disable update notifier messages
update-notifier=false
; Reduce verbose logging from npm during CI/local runs
loglevel=error
