# Example environment variables for MaycoleTracker backend
# Use PostgreSQL in production; SQLite is used by default when DATABASE_URL is unset.

DATABASE_URL=postgresql://user:password@localhost:5432/maycoletracker
SECRET_KEY=replace-with-a-secure-random-value
FLASK_ENV=production
