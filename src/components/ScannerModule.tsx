import React from 'react';

const ScannerModule = () => (
  <div>
    <h1>Barcode Scanner</h1>
    <p>Scan items to track inventory in real time.</p>
    {/* Future: integrate QuaggaJS or WebRTC scanner */}
  </div>
);

export default ScannerModule;